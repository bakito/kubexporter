receivers:
  otlp:
    protocols:
      grpc:
        endpoint: 0.0.0.0:4317
exporters:
  file:
    path: /etc/otel/output/metrics.json
service:
  pipelines:
    metrics:
      receivers: [otlp]
      exporters: [file]
